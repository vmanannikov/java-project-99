spring:
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:mem:dev
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console/
      settings:
        web-allow-others: true
logging:
  level:
    root: INFO
    web: DEBUG